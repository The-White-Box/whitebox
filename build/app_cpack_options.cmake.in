# Copyright (c) 2024 The WhiteBox Authors.  All rights reserved.
# Use of this source code is governed by a 3-Clause BSD license that can be
# found in the LICENSE file.
#
# CPack configuration.
#
# AUTOGENERATED FROM app_cpack_options.cmake.in.
# ALL MANUAL CHANGES WILL BE LOST.

SET(CPACK_PACKAGE_NAME ${PACKAGE_NAME})
SET(CPACK_PACKAGE_VERSION_MAJOR ${PACKAGE_VER_MAJOR})
SET(CPACK_PACKAGE_VERSION_MINOR ${PACKAGE_VER_MINOR})
SET(CPACK_PACKAGE_VERSION_PATCH ${PACKAGE_VER_PATCH})
SET(CPACK_PACKAGE_DESCRIPTION "WhiteBox is an open-source Source 1 rework project that aims to build a safer, faster, and more feature-rich Source 1 apps.
    Repository: https://github.com/The-White-Box/whitebox")
SET(CPACK_PACKAGE_DESCRIPTION_SUMMARY ${CPACK_PACKAGE_DESCRIPTION})
SET(CPACK_PACKAGE_CONTACT "Dmitry Tsarevich secure.whitebox.authors@gmail.com")
SET(CPACK_RESOURCE_FILE_LICENSE  ${WB_ROOT_DIR}/LICENSE)
SET(CPACK_PACKAGE_VENDOR "The WhiteBox Authors")

SET(CPACK_PACKAGE_ICON "${WB_ROOT_DIR}/apps/half-life-2/install\\half-life-2-install.png")
SET(CPACK_CREATE_DESKTOP_LINKS "half-life-2")
SET(CPACK_PACKAGE_EXECUTABLES "half-life-2" "Half-Life 2")
SET(CPACK_PACKAGE_INSTALL_DIRECTORY "${PACKAGE_NAME} ${PACKAGE_VER_MAJOR}.${PACKAGE_VER_MINOR}.${PACKAGE_VER_PATCH}")

# set the install/uninstall icon used for the installer itself
SET(CPACK_NSIS_MUI_ICON
    "${WB_ROOT_DIR}/apps/half-life-2/install\\half-life-2-logo.ico")
SET(CPACK_NSIS_MUI_UNIICON
    "${WB_ROOT_DIR}/apps/half-life-2/install\\half-life-2-logo.ico")
# set the add/remove programs icon using an installed executable
SET(CPACK_NSIS_INSTALLED_ICON_NAME "half-life-2.exe")
# set the name of the project in install app
SET(CPACK_NSIS_PACKAGE_NAME "${PACKAGE_NAME} ${PACKAGE_VER_MAJOR}.${PACKAGE_VER_MINOR}.${PACKAGE_VER_PATCH}")
SET(CPACK_NSIS_MENU_LINKS
    "https://github.com/The-White-Box/whitebox/tree/master/docs" "WhiteBox documentation"
    "https://github.com/The-White-Box/whitebox/issues" "WhiteBox issues tracker"
)
